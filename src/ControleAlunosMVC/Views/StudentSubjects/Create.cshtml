@model ControleAlunosMVC.Models.ViewModels.StudentSubjectViewModel
@{
    ViewData["Title"] = "Criar Matricula";
}

<h2>@ViewData["Title"]</h2>
<hr />
<div class="form-group row">
    <div class="col-md-4">
        <form asp-action="Create">

            <div class="form-group">
                <label asp-for="StudentSubject.StudentId" class="control-label"></label>
                <select asp-for="StudentSubject.StudentId" asp-items="@(new SelectList(Model.Students,"Id","Name"))" class="form-control"></select>
            </div>

            <div class="form-group">
                <label asp-for="StudentSubject.SubjectId" class="control-label"></label>
                <select asp-for="StudentSubject.SubjectId" asp-items="@(new SelectList(Model.Subjects,"Id","Name"))" class="form-control"></select>
            </div>

            <div class="form-group">
                <label asp-for="StudentSubject.DateStudentSubject" class="form-label"></label>
                <input asp-for="StudentSubject.DateStudentSubject" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="StudentSubject.Obersavation" class="control-label"></label>
                <input asp-for="StudentSubject.Obersavation" class="form-control" />
            </div>

            <div class="form-group">
                <br />
                <input type="submit" value="Criar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

